namespace Authentication
{
    public enum Error
    {
        NONE,
        EMAIL_HOST_UNREACHABLE,
        INTERNAL_ERROR,
        ACCESS_DENIED,
        EXISTS,
        NOT_FOUND,
        FORBIDDEN
    }
}
